import { DefaultTheme } from "@react-navigation/native"

const routeTheme = {
    ...DefaultTheme,
    colors : {
        ...DefaultTheme.colors,
        background : 'white'
    }
}


export default routeTheme;